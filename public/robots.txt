# PointVault — https://pointvault.app/
# All content beyond / and the marketing/legal pages is auth-gated, so
# crawlers can index the public pages but won't see anything private.

User-agent: *
Allow: /

# Service worker + Vite chunks are noise for indexing
Disallow: /sw.js
Disallow: /workbox-*.js
Disallow: /assets/

Sitemap: https://pointvault.app/sitemap.xml
